
<section class="gc-features">
	<h3>{{ learning[locale].upcomingActivities }}</h3>
	<div class="row wb-eqht-grd">
		{%- for event in learninglist -%}
		{%- if event.data.eventDate -%}
		<div class="col-sm-6">
			<section class="well well-sm">
				{%- if event.data.image -%}<img src="{{ event.data.image.source }}" alt="{{ event.data.image.alt }}">{%- endif -%}
				<h3><a class="stretched-link" href="{{ event.url }}">{{ event.data.title }}</a></h3>
				<dl>
					<dt>{{ learning[locale].eventDate }}</dt>
					<dd>{{ event.data.eventDate | htmlDateString }}</dd>
				</dl>
				{%- if event.data.description -%}
				<p>{{ event.data.description | markdownInline | safe }}</p>
				{%- endif -%}
			</section>
		</div>
		{%- endif -%}
		{%- endfor -%}
	</div>
</section>

<section class="gc-features">
	<h3>{{ learning[locale].scheduledActivities }}</h3>
	<div class="row wb-eqht-grd">
		{%- for event in learninglist -%}
		{%- if not event.data.eventDate -%}
		<div class="col-sm-6">
			<section class="well well-sm">
				{%- if event.data.image -%}<img src="{{ event.data.image.source }}" alt="{{ event.data.image.alt }}">{%- endif -%}
				<h3><a class="stretched-link" href="{{ event.url }}">{{ event.data.title }}</a></h3>
				{%- if event.data.description -%}
				<p>{{ event.data.description | markdownInline | safe }}</p>
				{%- endif -%}
			</section>
		</div>
		{%- endif -%}
		{%- endfor -%}
	</div>
</section>
